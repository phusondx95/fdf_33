<% if @suggest.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <%= t(".theform") %> <%= pluralize(@suggest.errors.count, t(".errors")) %>.
    </div>
    <ul>
    <% @suggest.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
