<% if @category.errors.any? %>
  <div class="error_explanation">
    <div class="alert alert-danger">
      <%= t(".theform") %> <%= pluralize(@category.errors.count, t(".errors")) %>.
    </div>
    <ul>
    <% @category.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
